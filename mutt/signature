#! /bin/sh

if ! hostname | grep "^verns-worktop" > /dev/null 2>&1; then
    echo 'Vern'
    echo 'vim -c ":s(^(ZnQr8va1puvan.(|:s)\d\(\S\)) \\U\\1)g|:1 normal g??"'
else
    case "$1" in
    jp)
        echo -n 'Sun Wuan (孫武安)'; LANG=ja_JP.UTF-8 && date +" %m.%d (%A)";;
    en)
        echo -n 'Sun Wuan (孙武安)'; LANG=en_US.UTF-8 && date +" %m.%d (%A)";;
    *)
        echo -n 'Sun Wuan (孙武安)'; LANG=zh_CN.UTF-8 && date +" %m.%d (%A)";;
    esac
    echo 'Development Dept. #1, FNST.(7998-8513)'
    echo '+86+25-86630566-8513, sunwuan@cn.fujitsu.com'
fi
