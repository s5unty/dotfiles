# 光标行为
# 阅读正文时的光标行为
bind pager      <backspace> previous-line
bind pager      k           previous-line
bind pager      j           next-line
bind pager      P           recall-message # 'R'ecall-message move to 'P'
bind pager      r           group-reply    # reply to all
bind pager      R           reply          # single reply
bind pager      g           list-reply     # reply to list
bind pager      .           next-entry
bind pager      ,           previous-entry
bind pager      b           previous-page
# 浏览首页时的光标行为
bind index      P           recall-message # 'R'ecall-message move to 'P'
bind index      r           group-reply    # reply to all
bind index      R           reply          # single reply
bind index      g           list-reply     # reply to list
bind index      N           search-opposite
bind index      -           half-up
# 浏览通讯录时
bind alias      N           search-opposite
# 切换邮箱时
bind browser    i           exit
# 查看附件时
bind attach     i           exit
