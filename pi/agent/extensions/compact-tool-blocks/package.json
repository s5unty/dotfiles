{
  "name": "pi-compact-tool-blocks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Remove vertical tool-shell padding while preserving inter-tool spacing in Pi 0.87.1",
  "pi": { "extensions": ["./index.ts"] },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "scripts": { "test": "node --test tests/*.test.mjs" }
}
